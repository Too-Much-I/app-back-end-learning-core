package web.tosunsaeng.domain.exams.domain.enums;

public enum ExamCreationState {
    PREPARED,
    RESERVED,
    SESSION_COMMITTED,
    SUCCEEDED,
    CANCEL_PENDING,
    CANCELED,
    EXPIRED,
    FAILED_TERMINAL
}
